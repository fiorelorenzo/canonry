node_modules/
dist/
build/
.svelte-kit/
.env
.env.*
!.env.example
*.log
.DS_Store
coverage/
.turbo/
# Scratch output a run produces and nobody should commit: benchmark results, rendered
# import corpora, anything an afternoon's evidence lives in. packages/bench already
# ignored its own, which left the repo root uncovered.
.data/
*.tsbuildinfo
